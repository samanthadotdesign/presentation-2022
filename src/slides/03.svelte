<script>
	/* PROPS */
	export { clazz as class };

	/* LOCAL VARS */
	let clazz;
	const slideConfig = $slidesIndex.Slide03;

	/* STORES */
	import { slidesIndex } from '@stores/layout';
</script>

<div class={`w-screen h-screen bg-gray ${clazz}`}>"SLIDE 3"</div>
